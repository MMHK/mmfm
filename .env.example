# 本地播放模式
# true: 浏览器端 HTML5 Audio 直接播放
# false: 使用后端远程播放（默认）
LOCAL_PLAYER_MODE=false

# 日誌級別: error, warn, info, debug (預設: info)
LOG_LEVEL=info

# === 後端服務配置 (WebService.ts) ===
# 監聽地址 (預設: 0.0.0.0)
HOST=0.0.0.0
# 監聽端口 (預設: 8011)
PORT=8011
# 前端靜態檔案路徑 (預設: ./public)
WEBROOT=./public
